# UI 08: 管理画面フォームUX

## 状態

実装・独立監査・検証済み

## 依存関係

- UI 07

## 目的

偵察、被弾表、DB確認、設定、兵種編集を横断し、入力の意味、実行前の結果予測、保存中・成功・失敗、破壊的操作の対象を迷わず把握できる状態へ整える。API、保存値、パーサー、権限、削除対象は変更しない。

## 現在の構成と責務

| 画面 | URL | 主な実装 | 責務 |
| --- | --- | --- | --- |
| 偵察検索 | `/warlords/scout` | `ScoutTab.tsx` | 複数名照合、TSV/150文字以内の報告文コピー |
| 被弾表 | `/warlords/damage` | `DamageTab.tsx`、`lib/action.ts` | 40分/80分閾値による状態判定、改名前後の統合 |
| DB確認 | `/warlords/db` | `DbTab.tsx`、`lib/warlordStats.ts` | 武将検索・並べ替え、能力値取込、TSVコピー |
| 兵種図鑑 | `/encyclopedia/units` | `UnitTab.tsx`、`UnitEditModal.tsx` | 兵種の検索、一括取込、追加・編集・削除 |
| 環境設定 | `/settings` | `SettingsTab.tsx`、`FactionTab.tsx` | テーマ、過去ログ、国色、データ整理・削除 |
| 配線・権限表示 | `/` | `app/page.tsx`、`lib/tabs.ts` | 公開/管理タブ、詳細画面、管理操作の表示 |
| 保存API | `/api/**` | `lib/api.ts`、Route Handler | JSON契約、認証、DB更新 |

`scout`、`damage`、`db`、`factions`は管理者専用タブで、`history`と`units`は公開タブである。更新APIはUIの表示制御とは独立してサーバー側の`requireAdmin()`で保護され、未認証時は401を返す。

## 維持する外部契約

| 機能 | 維持する契約 |
| --- | --- |
| 武将能力値取込 | `POST /api/warlord-stats`、body `{ stats }` |
| 兵種追加・更新 | `POST /api/unit-types`、bodyは12項目の`UnitType` |
| 兵種一括取込 | `PUT /api/unit-types`、body `{ units }` |
| 兵種削除 | `DELETE /api/unit-types/:name`、名前をURLエンコード |
| 国色保存 | `PUT /api/faction-colors`、差分でなく全色マップ |
| 偵察TSV | `国 / 武将名 / タイプ / 兵種タイプ / 兵種名`の列順 |
| 偵察報告 | `名前［短縮タイプ｜兵種名］`、150文字上限 |
| 被弾判定 | 40分未満、40〜80分、80分以上の閾値と並び順 |
| 兵種複合値 | `金:600`、`武力:40`、`36年`等の文字列表現 |
| 保存後の入力 | 成功時のみクリア、失敗時は入力と例外メッセージを維持 |

## 現状の良い点

- API認可がUIから独立しており、表示漏れが更新権限の突破にならない
- Historyの登録は事前解析、入力保持、部分失敗、二重送信防止が既に整っている
- DB/兵種取込のパーサーは純粋関数で、同名後勝ち、ヘッダー除外、不正行skipがテストされている
- `useModalA11y`はフォーカストラップ、Escape、開始位置への復帰、背景スクロール抑止を備える
- Unitの一括取込は、有効件数をボタン文言へ即時反映している
- 削除確認文は対象名と元に戻せないことを表示している

## 主な問題

| 重大度 | 問題 | 原因・影響 |
| --- | --- | --- |
| ★★★★★ | 未認証でも兵種詳細の「編集」が見える | 保存APIは401だが、利用者には実行可能に見える。表示を変えると既存のユーザー可視挙動も変わる |
| ★★★★★ | 兵種名変更がrenameでなく新規upsertになる | 旧名が残る。編集途中の名前で削除するため、対象の取り違えも起こり得る |
| ★★★★☆ | 削除失敗が前面の確認Dialogから見えない | エラーは背面の編集Dialogだけに描画され、回復方法を判断できない |
| ★★★★☆ | 取込前の部分成功が予測しづらい | DB取込は有効/除外件数がなく、兵種取込も除外件数を完了後まで示さない |
| ★★★★☆ | フォーム状態の意味付けが不足 | 成功/失敗のライブ通知、必須入力との関連、確認Dialogの説明関連付けがない |
| ★★★★☆ | 国色保存の競合・失敗回復が不明瞭 | ローカルへ即反映後に全件PUTし、失敗時に画面を戻さない。方式変更は保存挙動へ影響する |
| ★★★☆☆ | DB/被弾表のフィルターが独自実装 | 共通`FilterPanel`と開閉、解除、結果通知、フォーカス復帰が重複する |
| ★★★☆☆ | 破壊的操作がnative confirmとDialogに分散 | 一括置換すると同期/非同期、対象保持、フォーカスの挙動が変わる |

## テストの現状

既存:

- `lib/tabs.test.ts`: 公開/管理者タブ分類
- `lib/unitTypeForm.test.ts`: 12項目、複合値、同名、skip
- `lib/warlordStats.test.ts`: 能力値取込、同名、skip
- `lib/auth.test.ts`: セッショントークン
- `components/FilterPanel.test.tsx`: 開閉、結果通知、条件解除のARIA契約

不足:

- `lib/api.ts`のURL、method、JSONキー
- DB/兵種取込の事前状態、成功/失敗の意味付け
- Unit編集の入力名、必須状態、Dialog説明、削除失敗表示
- Scoutのコピー結果と読取専用出力のラベル
- 各管理フォームの320px表示
- 保存失敗、部分skip、削除失敗時のUI回帰

## 実装計画

| 順番 | 変更対象 | 変更理由 | 期待する効果 | 影響範囲 | 破壊的変更の可能性 | 確認方法 |
| ---: | --- | --- | --- | --- | --- | --- |
| 1 | `lib/api.test.ts`、既存parser test | 保存契約を先に固定する | UI変更中のURL/payload逸脱を検出 | テストのみ | なし | fetch mockでURL、method、bodyを比較 |
| 2 | DB/兵種の取込領域 | 実行前に有効・除外件数を知れない | 部分成功を予測し、貼り付けを修正できる | 表示、ARIA、CSS | 低。parserと実行条件は維持 | 有効、不正、同名、空入力を比較 |
| 3 | 共通のフォーム結果表示 | 成功/失敗が見た目だけに依存する | status/alertと入力説明を統一 | 取込2画面 | 低。文言と例外は維持 | SSR契約、手動読み上げ、二重通知確認 |
| 4 | `UnitEditModal` | 必須・エラー・削除説明の関連が弱い | 該当入力と回復方法を特定できる | 兵種追加/編集Dialog | 低。保存/削除対象は維持 | 初期DOM、空名、削除失敗、Tab/Escape |
| 5 | Scoutのコピー領域 | 出力欄と成功/失敗通知の名前が弱い | コピー対象と結果を区別できる | 偵察検索のみ | 低。生成文字列は維持 | TSV/報告文の文字列比較 |
| 6 | DB/被弾表の`FilterPanel`移行 | 同じ操作を個別実装している | 開閉、解除、件数、条件チップを統一 | 2一覧の表示層 | 中。フィルター値・初期openは維持 | 条件別件数・sort、320〜1440px |
| 7 | 全体検証・Before/After | 表示改善が保存契約へ波及していないか確認 | PR単位で安全に戻せる | UI 08全体 | なし | 全test、Lint、型、build、実画面 |

## このPRで変更しないもの

- 兵種名変更をrenameとして扱うこと
- 編集途中の名前と削除対象の契約
- API payload、レスポンスキー、HTTP method
- 新しい数値範囲や必須規則
- 部分成功を全件失敗へ変えること
- 国カラーを差分保存へ変えること、失敗時rollback
- native confirmの一括Dialog化
- 認証方式、Cookie、DBスキーマ
- History/ログインの既に安定しているフォーム構造

未認証の兵種詳細から「編集」を隠す案は権限の認知を正す一方、既存のユーザー可視挙動を変える。保存APIの認可は既に安全であるため、このPRでは実施せず独立案として残す。

## 受入条件

- 既存のURL、method、payload、複合値をテストで固定できる
- DB/兵種取込で、実行前に有効件数と除外件数が分かる
- 成功は`status`、失敗は`alert`として通知され、同じ内容を二重通知しない
- 兵種名の必須エラーが入力欄と関連付けられる
- 兵種削除の説明と失敗が前面の確認Dialogから分かる
- Scoutの2種類のコピー対象と結果を支援技術で区別できる
- DB/被弾表のフィルター値、初期open、件数、並び順が変わらない
- 320 / 390 / 768 / 1024 / 1440pxで横スクロールや主要操作の重なりがない

## 確認方法

- `npm test`
- `npm run lint`
- `npm run typecheck`
- `npm run build`
- `git diff --check`
- 未認証/管理者の表示境界
- URL直アクセスとブラウザ戻る/進む
- 有効入力、不正入力、同名、部分skip、通信失敗、削除キャンセル/失敗
- Tab、Shift+Tab、Escape、Enter、Cmd/Ctrl+Enter
- Light/Dark、320 / 390 / 768 / 1024 / 1440px
- 保存成功/失敗はfetchまたはブラウザ通信を置換し、実データを更新しない

## リスク

- UIテスト環境はNode中心で、ブラウザ操作は実画面確認を併用する必要がある
- parserの`parsed`は同名をまとめた後の件数であり、重複行数そのものは返さない
- Unit削除対象の仕様が曖昧なため、見た目の改善中も対象名を変更しない
- 国色は全件PUTのため、保存方式へ触れると競合/rollbackの仕様決定が必要になる
- native confirmを非同期Dialogへ変えると、確認中の対象を固定する追加設計が必要になる

## 実施結果

### 変更内容

- `lib/api.test.ts`で武将能力値取込、兵種取得・保存・一括取込・削除のURL、method、JSON payload、成功/失敗時のキャッシュ契約を固定した
- `ImportFeedback.tsx`を追加し、DB確認と兵種図鑑の取込前に有効件数・除外行数を表示し、成功を`status`、失敗を`alert`として扱った
- `DbTab.tsx`と`DamageTab.tsx`の独自フィルターを既存の`FilterPanel`へ統合し、初期展開、検索値、選択肢、解除、件数、並び順を維持した
- `UnitEditModal.tsx`を3つの責務別fieldsetへ整理し、Enter送信、二重送信防止、必須入力との関連付け、保存中状態、削除説明、削除失敗の表示先を明確にした
- 削除通信中に確認Dialogを閉じても、失敗を背面の編集Dialogへ引き継ぐようにした。確認Dialogが開いている間は前面だけに表示し、二重表示を避けた
- `ScoutTab.tsx`のTSVと150文字報告文を純粋関数へ分離し、文字列契約をテストで固定した。入力・出力の可視ラベル、件数、文字数、コピー結果を関連付けた
- `app/globals.css`へ取込状態、フォーム区分、必須エラー、Mobile 1カラムの局所スタイルを追加した
- 変更前後のLightテーマ実画面を`ui-08-before`と`ui-08-after`へ記録した。設定画面は調査・比較対象に含めたが、保存方式には触れていない

### 変更理由

- 取込前に部分成功の件数が分からず、不正行を含んだまま実行しやすかった
- 管理一覧ごとにフィルターの開閉、解除、件数通知が異なり、学習コストと重複実装が増えていた
- 兵種編集の12項目が平坦に並び、必須条件、複合値、失敗箇所の把握に時間がかかっていた
- 削除失敗が前面Dialogに表示されず、通信中にDialogを閉じると失敗情報も見えなくなる可能性があった
- 偵察コピーの生成規則がイベント処理内に埋まり、外部へ貼り付ける文字列を回帰テストできなかった

### 互換性

- `POST /api/warlord-stats`の`{ stats }`、兵種APIのURL、HTTP method、12項目payload、URLエンコード、成功時だけのキャッシュ失効を変更していない
- DB/兵種取込のparser、`parsed === 0`の実行条件、成功時だけ入力を消す挙動、失敗時に入力と例外情報を残す挙動を変更していない
- 偵察TSVの5列順、未登録表現、報告文の入力順、短縮規則、150文字上限を変更していない
- DB確認の同一家代表集約・更新日時降順、被弾表の40分/80分判定、各フィルター値とsortを変更していない
- 兵種保存は現在の編集値全体を送り、名前だけtrimする。削除対象も確認開始時点の`editing.name`のままで、rename仕様には変更していない
- URL、ルーティング、DBスキーマ、環境変数、認証Cookie、公開レスポンスキー、設定保存方式は変更していない

実画面確認の初回試行では、既存の「剛弓僧兵」へ同じ12項目を1回upsertした。名前、項目値、件数に差分はないが、`updatedAt`は2026-07-23 20:17:12 JSTに更新された。これを隠さず変更後READMEへ記録し、その後の保存・削除確認はブラウザ内で通信を置換して追加更新を防いだ。

### 検証結果

- 対象テスト: 5ファイル・21件成功
- 全テスト: 42ファイル・404件成功
- ESLint: warning / error 0
- TypeScript: `tsc --noEmit`成功
- Production build: Prisma Client生成、Next.js 14.2.35 build成功
- `git diff --check`: 成功
- 独立コード監査: blocker / high 0。削除失敗の引継ぎ、Label in Name、エラー色の3指摘を修正後に再検証した
- DB確認: `/warlords/db`を維持し、全169件、取込プレビュー「取り込み可能 1件 / 除外 1行」を確認した
- 兵種図鑑: `/encyclopedia/units`を維持し、全122件、同じ取込プレビューと既存一覧の表示を確認した
- 兵種編集: 必須エラーが名前入力へフォーカスし、`aria-invalid`と説明を関連付けることを確認した
- 削除失敗: `DELETE`をブラウザ内で500へ置換し、前面表示と、通信中に確認Dialogを閉じた後の背面表示を確認した。再確認中のwrite requestは0件だった
- 偵察報告: `因幡月夜［武｜剛弓］, ゴジラ［武政｜剛弓］, 名無しネコ［統｜スイス］`と150文字カウントを確認した
- Responsive: DB確認、兵種図鑑、偵察検索、被弾表を320 / 390 / 768 / 1024 / 1440pxで計20通り確認し、横方向overflow 0件だった

### Before / After

| 対象 | Before | After |
| --- | --- | --- |
| DB取込 | 実行結果だけを表示 | 実行前に有効・除外件数を表示し、可視ラベルと結果を入力へ関連付け |
| 兵種取込 | 有効件数だけをボタンへ表示 | 有効・除外件数、処理中、成功、失敗を同じ構造で表示 |
| DB/被弾フィルター | 画面固有の開閉・解除 | 共通FilterPanelの開閉、条件チップ、解除、結果通知へ統一 |
| 兵種編集 | 12項目を平坦に配置 | 基本情報、雇用・条件、特性・効果に分け、Desktop 2列 / Mobile 1列 |
| 削除失敗 | 背面だけに表示 | 現在見えているDialogへ表示し、閉じても情報を失わない |
| 偵察コピー | 出力名と残り容量が不明瞭 | 可視ラベル、件数、`現在/150文字`、独立した成功/失敗通知を表示 |

変更前は`docs/ui-redesign/ui-08-before/`、変更後は`docs/ui-redesign/ui-08-after/`で同じURL・期・管理者表示・Lightテーマを比較できる。

### 残っているリスク

- 未認証でも兵種詳細の「編集」が見える既存挙動は、ユーザー可視仕様を変えるため実施していない。更新APIの401認可は維持される
- 兵種名変更はrenameでなく新規upsertになる。旧名の扱いと削除対象を決める仕様変更が必要なため実施していない
- 国色保存の全件PUT、競合、失敗時rollbackは保存契約へ影響するため実施していない
- native confirmの一括置換は同期/非同期と対象固定の挙動を変えるため実施していない
- Node中心のコンポーネントテストでは非同期Dialog遷移を直接操作していないため、通信置換によるブラウザ回帰確認を併用している

## ロールバック

作業単位ごとに、テスト、取込表示、Unit Dialog、Scout、FilterPanel移行の順で独立コミットする。問題があれば表示・フィードバック層だけを直前の構造へ戻し、parser、API、DB、認可は維持する。
