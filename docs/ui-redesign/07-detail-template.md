# UI 07: 詳細画面テンプレート

## 状態

実装・ローカル検証完了（Draft PR #146）

## 依存関係

- UI 06

## 目的

武将、兵種、武器、品物、国で異なる詳細画面の構造を、共通の見出し、主要指標、Section、空状態へ揃える。

## 変更対象

- `DetailPage`による外枠と見出しの意味付け
- `DetailSummary`による主要指標と勝敗バーの共通化
- `DetailEmptyState`によるタイトル・説明付き空状態の共通化
- `DetailBattleLogSection`による最近の戦闘枠の共通化
- 320pxでもタイトルを潰さないレスポンシブヘッダー
- 推移グラフのデータに基づく文章要約

## 対象外

- 詳細URL、集計値、グラフ計算、関連項目の順序
- API、DB、ブラウザ履歴スタックの仕様
- Sectionの開閉状態、年フィルターの適用範囲、表示件数
- 管理者限定機能と兵種編集の権限制御

## 互換条件

- すべての詳細URLと既存共有リンクを維持する
- 表示する値、母数、並び順を変更しない
- 戻る操作と相互リンクの挙動を維持する
- 武器は`equip2`、品物は`equip1`を集計する内部互換を維持する
- 武将と他4種で異なる両陣営一致時の計上方法を維持する
- 武将だけが使用兵種と戦闘ログで年フィルターを共有する挙動を維持する

## 調査結果

- 5種の詳細画面は、武将、兵種、武器・品物共通、国の4コンポーネントで実装されている
- `DetailHeader`、`StatCards`、`WinRateBar`、`Section`、`BattleLogList`は既に共通化されている
- 未共通なのは外枠＋ヘッダー、主要指標＋勝敗バー、空状態、戦闘ログSectionである
- 1440pxの代表画面は横方向の破綻がなく、既存の読書幅と情報密度を維持できる
- 320pxの兵種詳細では、戻る・タイトル・編集・共有を1行に置くためタイトル領域が44pxまで縮み、4文字の兵種名が3行になる
- 詳細内の「戻る」は`window.history.back()`ではなく内部`detailStack`を1件減らす操作である
- URLは現在の詳細だけを表し、相互遷移の途中履歴は`history.state`にのみ保持される
- 武将、兵種、国には固有状態・権限制御・非同期取得があり、Section配列への設定駆動化は責務を曖昧にする

## 変更理由

既存UIの良い点である情報量、固有分析、読書幅を維持しながら、同じ見た目を各画面が個別に組み立てる重複を減らす。狭幅では操作を最初の行、現在地を次の行へ分け、タイトルを読むための幅を確保する。集計や画面固有状態は共通部品へ移さず、共通化の責務を表示構造だけに限定する。

## 期待する効果

- 5種で外枠、戦績サマリー、空状態、最近の戦闘の見出し階層が揃う
- 空状態や主要指標を追加・修正する際の変更箇所が減る
- 320pxでも詳細名を自然な幅で読める
- グラフを視覚だけに依存せず、データの開始・終了・最大値を文章で把握できる
- 各詳細画面は固有の集計・関連項目・管理操作に集中できる

## 影響範囲

- `components/detail/DetailParts.tsx`
- `components/detail/WarlordDetail.tsx`
- `components/detail/UnitDetail.tsx`
- `components/detail/EquipDetail.tsx`
- `components/detail/FactionDetail.tsx`
- 推移グラフを持つ`UnitInsights.tsx`と`WarlordInsights.tsx`
- 詳細画面に限定したCSSと回帰テスト

## 実施順

1. 5種のURL往復、旧共有リンク、武器・品物の内部対応を回帰テストで固定する
2. 共通外枠、戦績サマリー、空状態、戦闘ログSectionを追加する
3. 固有状態の少ない武器・品物から、兵種、国、武将の順に移行する
4. 320px以下を含むヘッダーのレスポンシブ配置を整える
5. 推移グラフの既存計算値から文章要約を生成する
6. 値、Section順、件数、URL、戻る、相互リンクを変更前後で比較する

## 受入条件

- 5種類の詳細画面で同じ情報階層を利用する
- 主要指標は既存の4〜5件と同じ値・順序を保つ
- 見出し階層が現在の詳細種別と名前を正しく示す
- 欠損・空状態が同じDOM構造と視覚言語を利用する
- 320pxでタイトルが操作群に押し潰されず、横スクロールが発生しない
- 推移グラフにデータ内容を説明するアクセシブルネームがある

## 確認方法

- 各詳細種別の代表データと空状態
- URL直アクセス、相互遷移、戻る操作
- 変更前後の値・順序比較
- 320 / 390 / 768 / 1024 / 1440pxの実画面比較
- 5種のパスと旧クエリの回帰テスト
- 集計・Lint・型チェック・本番ビルド

## リスク

- 戻るボタンをブラウザ履歴へ置換すると、詳細内の相互遷移履歴が変わる
- 共通部品へ`key`を付けると、同種別の対象切替時にSection等のローカル状態がリセットされる
- 年フィルターを一律化すると、武将と他4種で現在異なる適用範囲が変わる
- 「1戦闘1件」へ統一すると、兵種・武器・品物・国の両側一致時の件数が変わる
- 武器・品物の内部列対応を一般化すると、既存データの意味が逆転する
- パンくずから履歴途中へ直接遷移させるには、現行URLにない状態の公開仕様化が必要になる

## ロールバック

共通テンプレートを各詳細画面の既存構造へ戻す。statsとAPIには触れない。

---

## 実装結果

### 変更内容

- `components/detail/DetailParts.tsx`
  - 外枠と見出しを関連付ける`DetailPage`を追加
  - 既存の指標カードと勝敗バーを包む`DetailSummary`を追加
  - 文言と追加導線を呼び出し側に残す`DetailEmptyState`を追加
  - 件数、強調対象、年フィルターをそのまま渡す`DetailBattleLogSection`を追加
- `WarlordDetail.tsx`、`UnitDetail.tsx`、`EquipDetail.tsx`、`FactionDetail.tsx`
  - 武将、兵種、武器、品物、国の5種を共通テンプレートへ移行
  - 各画面固有の集計、Section、状態、管理操作は元のコンポーネントに維持
- `BattleLogList.tsx`
  - リンク操作のクリック領域と、対象戦闘を特定できるアクセシブルネームを改善
  - コピー完了を`role="status"`で通知
- `UnitInsights.tsx`、`WarlordInsights.tsx`
  - 推移グラフとヒートマップへ、実データから生成する文章要約を追加
  - 詳細内の相性ランキング用CSS名をランキング一覧のCSSから分離
- `app/globals.css`
  - 詳細ヘッダーをGrid化し、600px以下では操作行とタイトル行を分離
  - 勝敗・グラフ色をLight/Dark共通のテーマトークンへ移行
  - 戦闘ログ操作をDesktop 36px、Mobile 44pxの操作領域へ拡大
- 回帰テスト
  - 5種のURLと旧共有パラメータ
  - 武器=`equip2`、品物=`equip1`の集計対応
  - 両陣営一致時の計上と同一戦闘の重複排除
  - 共通テンプレート、空状態、Section順、グラフ要約

### 変更理由

外枠、戦績、空状態、戦闘ログという同じ表示責務を5種が個別に組み立てていたため、画面間の見出し構造と保守箇所が分散していた。表示構造だけを小さな共通部品へまとめ、集計や固有状態は移動しないことで、既存設計を保ちながら変更箇所を減らした。

狭幅では操作と現在地が同じ1行を奪い合っていたため、Fittsの法則に沿った44pxの操作領域を確保しつつ、タイトルを独立した次の行へ配置した。グラフには色や形だけでは得られない期間・最大値・最高/最低時間帯の文章要約を加えた。

### 互換性

- 公開API、DBスキーマ、環境変数、URL、ルーティングは変更していない
- 直リンクは`/warlord/:name`、`/unit/:name`、`/weapon/:name`、`/item/:name`、`/nation/:name`を維持した
- 旧共有パラメータ`w`、`u`、`wp`、`it`、`f`の解釈を回帰テストで固定した
- 5種すべてで表示値、Section順、表示件数、フィルター適用範囲を変更していない
- 詳細内の戻る操作は、ブラウザ履歴ではなく既存の`detailStack`を戻す挙動のまま
- 共通外枠に`key`を付けず、Sectionや年フィルター等のローカル状態を保持した
- 武器・品物の内部列対応と、武将/他4種で異なる両陣営一致時の計上方法を維持した
- 認証を伴う編集・保存APIや外部サービスには書き込みを行っていない

実画面で比較した代表値は次のとおりで、変更前後で一致した。

| 種別 | 対象 | 戦闘数 | 勝利 | 敗北 | 勝率 | 撤退・引分 |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| 武将 | 因幡月夜 | 271 | 155 | 90 | 63.3% | 26 |
| 兵種 | 剛弓僧兵 | 447 | 216 | 189 | 53.3% | 42 |
| 武器 | リーチ棒 | 558 | 249 | 227 | 52.3% | 82 |
| 品物 | 金の宝珠 | 3,273 | 1,350 | 1,248 | 52.0% | 675 |
| 国 | 天下五剣 | 6,988 | 3,278 | 2,857 | 53.4% | 853 |

### 検証結果

- `npm test`: 37ファイル、383テスト成功
- `npm run lint`: 警告・エラーなし
- `npm run typecheck`: 成功
- `npm run build`: Prisma Client生成、Next.js本番ビルド成功
- `git diff --check`: 成功
- 320 / 390 / 768 / 1024 / 1440px: 横スクロールなし
- 5種の直接URLと未知の名前による空状態: 文言・URLとも維持
- 武将→兵種→詳細内の戻る、ブラウザの戻る/進む、Escape: 既存の遷移を維持
- 戦闘ログのコピー/別タブ操作: コピー文字列と遷移URLが変更前と一致
- 変更前: [`ui-07-before/`](ui-07-before/)
- 変更後: [`ui-07-after/`](ui-07-after/)

### Before / After

| 観点 | Before | After |
| --- | --- | --- |
| 5種の外枠 | 各詳細コンポーネントが個別に構築 | `DetailPage`へ統一し、固有内容は子要素として維持 |
| 戦績構造 | カードと勝敗バーを各画面で個別配置 | 可視見出し「戦績サマリー」と同じ意味構造へ統一 |
| 320pxの兵種名 | タイトル幅44px、3行 | タイトル幅274px、1行 |
| 戦闘ログ操作領域 | 約15pxのアイコン領域 | Desktop 36px、Mobile 44px |
| グラフ | 汎用ラベルまたは視覚情報のみ | 始点・終点・最大値、最高/最低時間帯をデータから説明 |
| 勝敗文字のコントラスト | 色値が個別定義 | Light 5.74:1 / 5.84:1、Dark 8.98:1 / 6.38:1 |
| グラフ・バーのコントラスト | テーマ間の保証なし | 両テーマで最低4.06:1 |

### 残っているリスク

- アプリ全体の`main`は一覧タブを示す`role="tabpanel"`のまま詳細画面も内包する。詳細表示中のアクセシブルネーム整理はページ全体のタブ契約に関わるため、UI 09で扱う
- URLは現在の詳細だけを表すため、内部スタックの途中をすべてクリック可能なパンくずにすると公開状態が増える。今回は現在地の見出しと戻る操作を維持した
- 兵種の「編集」が未ログインでも表示される既存挙動は変更していない。書き込みAPIの認可は維持されるが、表示権限の整理はUI 08で確認する
- 管理者限定のカード保存、ウォッチ、兵種編集について、未ログインでの非破壊確認は実施したが、実データへの書き込み操作は実行していない
- Sectionの開閉初期値、年フィルターの適用範囲、相性ランキングの最低母数・並び順は仕様変更になる可能性があるため変更していない
